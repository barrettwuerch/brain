# 2026-02-26

- Block 5 completed and committed at 03c9d30 (gates 1–4 passed, Part B wired, smoke tests green).
- GitHub repo created/pushed: https://github.com/barrettwuerch/brain
- Railway deployment configured and live (us-east4). Root directory set to projects/brain. Start command npm run loop.
- Service running loop: tsx src/scripts/run_loop.ts. Verified build succeeded and deploy logs show first tasks executing (challenge_strategy episode written).
- Kalshi configured to demo on Railway (KALSHI_ENV=demo, KALSHI_BASE_URL=https://demo-api.kalshi.co).
- Note: Node 18 deprecation warning from supabase-js; upgrade to Node 20 to address in Block 6 (railway.toml / nixpacks).

Priority update:
- Next step is Front Office build (animated office UI) to provide live visibility during Block 6.
- After Front Office: Block 6 paper trading with real market data.

- Next milestone checks: 30-minute stability check for Railway (no crash/restarts) and that episodes accumulate.

## Later updates (evening)
- Front Office deployed on Vercel. Animated isometric Trading Office floor is live at /floor; Vercel auto-deploy was flaky and was worked around via Vercel CLI. Root Directory had to be cleared to repo root; deploys required git author email to match Vercel team (barrett.wuerch@gmail.com).
- Front Office improvements shipped:
  - Server-side Supabase reads only (service role); no anon key in browser.
  - /api/loop-health returns {lastEpisodeAt, minutesAgo, healthy} and nav shows green/red dot.
  - /api/simulation-stats wires scoreboard in Trading Office to real simulation capital (starting $5k) and positions-based P&L.
  - /scoreboard uses simulation capital, adds Available Capital, Trade History table via /api/trades.
  - Mobile responsiveness across all screens; /floor canvas now scales on mobile; desktop sidebar hidden on mobile.
- Simulation scripts added to brain:
  - simulation_baseline.ts publishes operational_state domain=simulation keys, including capital split Kalshi $1k + Alpaca cap $4k (total $5k).
  - seed_watch_conditions.ts seeds 5 Kalshi + 3 crypto watch conditions; later changed to alert_only defaults.
- Brain runtime fixes:
  - run_loop.ts changed to keep worker running when queue is empty; later added periodic loop_heartbeat episodes while idle.
  - Scanner Gate 0 issues fixed for Kalshi demo: removed hardcoded elections host; uses authenticated Kalshi client base URL and demo-friendly freshness heuristic (close_time + liquidity signals). Also fixed ticker selection to only prediction watch conditions.
  - Prevent test market contamination: deleted all KXBTC15M-TEST rows from positions; added guard in src/db/positions.ts to skip inserting any ticker containing 'test'.
- Visibility improvement (Option B): implemented scanner gate-block logging.
  - Added SQL file projects/brain/docs/scanner_gate_events.sql to create scanner_gate_events table.
  - Scanner logs every gate block to scanner_gate_events.
  - Front Office: /api/gate-stats + Pipeline screen section "Scanner Gate Blocks (24h)".
