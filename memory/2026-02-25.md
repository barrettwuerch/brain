# 2026-02-25

- Confirmed committed state:
  - c0da4db: Block 3c fixes 1–9
  - af78f31: Block 4 knowledge library (52 chunks) + reasoning templates
  - cbc6233: replaced hardcoded challenge_strategy stub with real LLM call (graceful fallback)
  - 635cf3c: implemented semantic/procedural memory writes
- Knowledge library injection in loop.ts filters by domain (shared + desk) and applies_to parsed from JSON stored in knowledge_library.source.

## Keys / env wired (projects/brain/.env)
- Added ANTHROPIC_API_KEY; verified challenge_strategy now produces real LLM report and persists to episodes.action_taken.challenge (example failure_probability=0.78, verdict=abandon).
- Added Kalshi creds as KALSHI_KEY_ID + KALSHI_PRIVATE_KEY (PEM). Note: current Kalshi adapter uses only public endpoints; auth wiring is Block 5. User will rotate keys before going live; treat pasted key as compromised.
- Added Alpaca paper creds; corrected ALPACA_BASE_URL to https://paper-api.alpaca.markets (not /v2). Verified GET /v2/account succeeds; account ACTIVE buying_power 200k.
- Resend: initial env names mismatch; cos_email.ts uses COS_EMAIL_FROM/COS_EMAIL_TO. Set COS_EMAIL_FROM=onboarding@resend.dev and COS_EMAIL_TO=barrett.wuerch@gmail.com; sendDailyBrief succeeded with message id ce8906e9-0d37-4e4b-9ef2-08d7df52674e.

## Fixes / changes made during Block 5 kickoff
- BRAIN_TEST_MODE set to false for Block 5.
- Removed OpenAI dependency for embeddings: src/lib/embeddings.ts now uses Voyage API if VOYAGE_API_KEY present, else deterministic hash-based 1536-dim embeddings (TODO Block 6 to wire Voyage properly). This unblocks monitor_approved_findings without OPENAI_API_KEY.
- Added robust fallback in agent/loop.ts reason(): if Anthropic JSON parse fails, log warning and fall back to safe deterministic action mapping (prevents loop crashes on malformed JSON).
- Added orchestrator gate: routing.ts routeUnroutedFindings now skips routing findings with pending validate_edge_mechanism tasks (queued/running).

## Memory write TODOs resolved
- semantic.ts and procedural.ts originally had empty writeSemanticFacts/writeProcedure; implemented.
- Important: writes must use supabaseAdmin (service role) due to RLS. Verified by inserting test rows (semantic_facts count +1; procedures count +1).

## Block 5 Gate 1 notes
- Seeded synthetic Gate 1 findings; validate_edge_mechanism task completed and wrote an episode. However, new synthetic finding ab8f5d72... did not get routed to strategy tasks yet; investigating routeUnroutedFindings behavior and task eligibility.
- identifyUnroutedFindings uses existing formalize_strategy tasks’ task_input.finding.id to mark routed; no DB jsonpath query.
