#!/usr/bin/env node
/**
 * sim_replay.mjs (stub)
 *
 * Goal: replay historical games quickly to validate paper trading without waiting weeks.
 *
 * TODO:
 * - Read dataset + pull candlestick series per game
 * - Step through minute candles and feed into engine deterministically
 * - Emit the same JSONL decision logs as live mode
 */

console.log('sim:replay not implemented yet (stub).');
process.exit(2);
