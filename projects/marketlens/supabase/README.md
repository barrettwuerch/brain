# Supabase

- Apply schema: see `../docs/DB_SCHEMA_SUPABASE.sql`
- Migrations live in `supabase/migrations/`

Initial approach: run SQL in Supabase SQL editor, then migrate to tracked migrations once stable.
